# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Production source maps, moved out of dist/ by scripts/stash-sourcemaps.mjs so
# they never reach the app bundle. Keep the matching sourcemaps/<version>/ folder
# with each release artifact — `pnpm symbolicate` needs it to read a user's stack
# trace — but it holds the full frontend source, so it doesn't belong in git.
sourcemaps

# TanStack Router plugin cache
.tanstack

# santree's own worktrees. The app creates `<repo>/.santree/worktrees/<id>` in
# every repo it manages, this one included when dogfooding — a nested checkout is
# not content of the parent checkout.
.santree/

# Claude Code: session/worktree state stays local, but shared project config
# (settings, hooks, skills, agents, commands) is versioned so every clone and
# every santree-managed worktree inherits it.
.claude/*
!.claude/settings.json
!.claude/hooks
!.claude/skills
!.claude/agents
!.claude/commands

# Rust / Cargo (workspace target)
/target

# Editor directories and files. Keep the shared workspace recommendations and
# settings (the Tailwind/Biome fix) tracked; ignore everyone's personal files.
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# NOTE: src/bindings.ts and src/routeTree.gen.ts are GENERATED but committed,
# so they are intentionally NOT ignored — release builds rely on them.

# Website (website/ is a standalone nested pnpm project). Unlike the app's
# committed src/routeTree.gen.ts (NOTE above), the website's route tree is
# regenerated by `tsr generate` on every typecheck/build, so it stays out of
# git. Its build output (website/dist) is covered by the unanchored `dist`
# rule above.
website/src/routeTree.gen.ts
*.tsbuildinfo

# pnpm writes a store here when it refreshes deps non-interactively (which a
# version bump triggers). Build cache from a validation run, never product code
# — ignored so a broad `git add` during a release cut can't sweep it in.
.pnpm-store/
